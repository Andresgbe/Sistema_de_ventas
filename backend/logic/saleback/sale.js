class Sale {
  constructor(codigo_producto, cliente_id, descripcion, cantidad, total) {
    this.codigo_producto = codigo_producto;
    this.cliente_id = cliente_id;
    this.descripcion = descripcion;
    this.cantidad = cantidad;
    this.total = total;
  }
}

export default Sale;
